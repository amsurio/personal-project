import React, { FC } from 'react';

type OrderType = {
    order: any
}

const Orders: FC<OrderType> = ({ order }) => {
    //console.log(order);

    return (
        <div>

        </div>
    )
}
export default Orders